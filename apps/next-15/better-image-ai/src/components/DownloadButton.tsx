"use client";
import {
  DownloadButton as ShadcnDownloadButton,
  DownloadButtonProps,
} from "@repo/shadcn/download-button";

export const DownloadButton = (props: DownloadButtonProps) => {
  return <ShadcnDownloadButton {...props} />;
};
